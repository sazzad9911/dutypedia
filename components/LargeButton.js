import React from 'react';
import {TouchableOpacity,Text} from 'react-native'

const LargeButton = () => {
    return (
        <TouchableOpacity>
            
        </TouchableOpacity>
    );
};

export default LargeButton;